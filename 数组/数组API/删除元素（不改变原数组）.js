// 删除数组 arr 最后一个元素
function truncate(arr) {
    var arr1 = arr.slice(0,arr.length-1);
    return arr1;
}
// 删除数组 arr 第一个元素
function truncate(arr) {
    var arr1 = arr.slice(1,arr.length);
    return arr1;
}