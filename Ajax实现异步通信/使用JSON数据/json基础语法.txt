1.
json结构由下面三种类型的数据组成
值：表示字符串、数值、布尔值、对象、数组和null，不支持undefined

对象：一组无序键/值对，{}，键值之间用“：”，键/值对之间用“，”
例：{"name":"xy", "age":"1"}

数组：一组有序的值列表，[]，值直接使用“，”隔开。
例：[1,true,"true"]

2.json对象的属性必须加双引号，不能使用单引号

3.属性的值可以是简单值，也可以是复杂类型值（对象）。