//参数增强
//调用函数时，给形参传了实参值，则首选用户传入的实参值。
//如果没有给形参传是实参值，则形参默认启用=右边的默认值。

function fn(a, b, c=0) {
	//es6之前设置参数的默认值方式
	//c === undefined && (c=0);
	console.log(a+b+c);
}

fn(10000,2000,500);
//未出现的实参就会使用默认值
fn(10000,2000);//1000,2000,0
fn(10000);//NaN