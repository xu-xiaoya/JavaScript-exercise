(1)如果只在调用一次函数时，临时替换其中的this，首选.call()
如果在这一次临时调用函数时，实参值是放在一个数组中整体传入的，而调用的函数形参却要求单独插入多个实参值，
就需要将.call()化成apply()，apply()打散数组实参后，再传给函数形参变量.
(2)反复调用函数和替换this，用bind()生成一个永久替换了this为指定对象的函数副本,反复使用函数副本
(3)回调函数中的this如果不是想要的，必须用bind来换.
